{
  "name": "test",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "author": "Gabriel Castro <dev@GabrielCastro.ca>",
  "license": "MIT",
  "devDependencies": {
    "jest": "^20.0.4",
    "neon-cli": "^0.2.0"
  },
  "scripts": {
    "build": "neon build",
    "build:debug": "neon build --debug",
    "test": "neon build --debug && jest -i"
  },
  "jest": {
    "verbose": true,
    "cache": false,
    "watchman": false
  }
}
